<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://i.ibb.co/WPZtpj8/icon.png" rel="icon" type="image/x-icon">
    <title>레시피컬렉션</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        html, body {
            height: auto;
            min-height: 100%;
        }
        body {
            background-color: #fff8e4;

            display: flex;
            flex-direction: column;

            padding-top: 20px;
            padding-bottom: 200px;
        }
        .card-img-top {
            height: 200px;
            object-fit: cover;
        }
        .backimg {
            background-image: url(https://i.ibb.co/3RC981V/FHD-background-imagex.png);
            background-repeat: repeat-x;
            background-position: bottom;
            height: 500px;
            width: 100%;
        }
    </style>
</head>
<body>
    <%-include('header.ejs')%>

    
    <div class="container" style="flex: 1;">
        <div class="row" id="recipe-cards">
            <% if (posts.length > 0) { %>
                <% posts.forEach(post => { %>
                    <div class="col-md-4">
                        <div class="card mb-4" onclick="location.href='/posts/view/<%= post.id %>'">
                            <img src="<%= post.imgpath ? post.imgpath : 'https://i.ibb.co/tbhWSCL/dish2.png' %>" onerror="this.onerror=null; this.src='https://i.ibb.co/tbhWSCL/dish2.png'" class="card-img-top">
                            <div class="card-body">
                                <h5 class="card-title">
                                    <%= post.title.length > 10 ? post.title.substring(0, 10) + ' ...' : post.title %>
                                </h5>
                                <p class="card-text">
                                    <%= post.content.length > 25 ? post.content.substring(0, 25) + ' ...' : post.content %>
                                </p>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="card" style="width: 18rem;" onclick="location.href='/write'">
                    <img src="https://i.ibb.co/tbhWSCL/dish2.png" class="card-img-top">
                    <div class="card-body">
                      <h5 class="card-title">요리를 추가해 주세요!</h5>
                      <p class="card-text">상단의 레시피 작성 버튼을 누르면 레시피를 추가할 수 있습니다.</p>
                    </div>
                  </div>
            <% } %>
        </div>
    </div>
    <div class="backimg"></div>

    <%-include('footer.ejs')%>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
